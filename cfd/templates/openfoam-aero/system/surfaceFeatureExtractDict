FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      surfaceFeatureExtractDict;
}

// Рёбра выделяются по углу: у корпуса это транец и скула, у крыла — задняя
// кромка. Задняя кромка важнее всего: размазанная кромка сдвигает точку схода
// и меняет циркуляцию, а значит и всю поляру.
//
// Запись идёт по КАЖДОМУ телу случая, а не только по первому: у составного
// случая (корпус и руль) рёбра второго тела иначе не извлекались бы вовсе.
// Результат — файлы `.eMesh`, и сеточник берёт их из списка `features` в
// snappyHexMeshDict; без этого списка вся работа здесь пропадает впустую.

{{extract_block}}
