# План: генакер против современных методов (2026-09-11)

Что делать с моделью генакера дальше, если сверить её допущения с тем, как
паруса и мембранные крылья считают сегодня. Документ писан ДО работы. Всё, что
названо числом, взято из кода, из `docs/wake.md`, из плана
`docs/gennaker-planing-plan.md` или из статей по ссылкам в конце; что оценка,
помечено оценкой.

Главный вывод сверки: наша модель стоит на том же классе методов, что и быстрые
аэроструктурные модели кайтов и парусов последних лет (нелинейная вихревая
решётка, сцепленная с вязкими 2D-полярами, плюс мембранная форма). Разрыв не в
классе метода, а в пяти местах, и план идёт по ним в порядке «дёшево и
измеримо» → «дорого и спорно».

---

## 0. Сверка допущений

| у нас | в литературе | что из этого следует |
|---|---|---|
| Полоска либо в решётке, либо вынута с предписанной Γ из поляры; переключение по геометрическому углу 35→45° с гистерезисом ([aero.js](../sim/aero.js) `LAT_OUT`/`LAT_IN`) | Vortex Step Method (TU Delft): полоска ВСЕГДА в уравнении, Γ итерируется к 2D-поляре с релаксацией; пелена у неё предписанная (подкова), не свободная | переключатель заменить не на смесь (отвергнуто девять раз, §5.12), а на предписанную пелену у сорванной полоски |
| Разнос в полосе гашения: колебание явной связи со ВТОРЫМ рядом пелены, растёт при мельчении шага (`docs/wake.md`, «Полоса 5.8…6.6 разобрана») | классическое правило UVLM (Katz & Plotkin): длина кольца пелены соразмерна хорде панели, Δt ≈ c_панели/V | у нас кольцо 0.22 м при панели 0.8 м, вчетверо мельче; это записанный долг «не хватает неявности» |
| Знак пуза `camSign = sign(alpha)` переключается на нуле угла ([aero.js:954](../sim/aero.js:954)); одна красная клетка карты 120°/7.5 (§4.13.6) | LESP (Ramesh): нагрузка передней кромки как непрерывная мера, а не геометрический угол | параметр уже считается: `sectionShape(q).a0` в [membrane.js:80](../sim/membrane.js:80) |
| За измеренным краем ±24° продолжение нормальной силой плоской пластины, C_N ≈ 1.28·sin α ([polar.py](../src/sv20/polar.py) `_deep_cl`) | на спинакере у топа устойчивый вихрь передней кромки (LEV) даёт 10…25 % секционной подъёмной силы; ниже сходит периодически; аналогия Полхамуса даёт добавку K_v·sin²α·cos α | продолжение занижает верхние полоски генакера; добавка одна константа на ряд поляры, но калибровать не по чему без п. 4 |
| Свидетель по силам ЦЕЛОГО паруса отсутствует: этап 2б CFD не выполнен, ходовая калибровка одна точка | опубликованы трубные силы и полётные формы асимметричных спинакеров по AWA и триму (Viola & Flay; Graf, Renzsch и др. как эталон для FSI) | без кривой C_x, C_y(AWA) пункты про точность останутся неизмеренными |
| Решётка по ПРОЕКТНОМУ пузу, мембранное пузо только в силы, обратно в геометрию не идёт ([aero.js:962](../sim/aero.js:962)) | слабая связь форма↔нагрузка с релаксацией сходится устойчиво без итераций внутри шага (FSI генакера, JMST 2018) | замкнуть между кадрами с недорелаксацией; цена ноль, матрица и так строится каждый шаг |
| Состояния «сложился» нет; заполаскивание множителем к пузу | в натуре flapping даёт +50…70 % динамической нагрузки; трим держат туже, чем в трубе, чтобы не сложился | для поляры второстепенно, для ощущения управления важно; последним |
| RANS/DES/LES | точность сил 0.5 % против трубы (RANS), LES в 50 раз дороже | в реальном времени недостижимо; только как эталон для п. 4 |

Что здесь НЕ предлагается, потому что уже отвергнуто замером и записано:
запаздывание, гистерезис или недорелаксация гашения (§5.12, девятая версия);
смесь двух постановок в одном уравнении (одиннадцатая); вынимание из матрицы без
силовой половины (двенадцатая); сужение полосы 41…45°; мельчение шага.

---

## 1. Правила плана

Каждый шаг записан одинаково: правка, свидетель, ворота, цена, что делать при
красном. Порядок обязателен: шаги 1…3 чинят устойчивость и снимают известные
дефекты, шаг 4 даёт эталон, шаги 5…6 повышают точность и меряются по эталону,
7…9 условные.

Правила проекта действуют без исключений: число из прогона на каждое
утверждение; отвергнутая правка идёт в `docs/wake.md` с числами; порог не
двигается под ответ; отпечаток `node scripts/golden.mjs` там, где правка не
должна ничего менять.

Свидетели, которые относятся ко всем шагам:

- карта курс×шкот в `tests/gennaker.test.mjs` (25 клеток, TWA 120…180°, шкот
  3.5…7.5 м, скачок тяги за шаг < 5 %, Γ < 300). Базовая линия на HEAD
  `d486795`: 1 красная клетка (120°/7.5);
- отпечаток на бейдевинде, левентике и поляре 45…120°: побайтово, пока правка
  не трогает срывной режим;
- `make test` зелёный, `wake`, `sailcoeffs`, `wind` в регрессионном режиме.

---

## 2. Шаги

### Шаг 0. Базовая линия и цена третьего паруса

**Правка.** Никакой. Зафиксировать числа, от которых считаются все остальные
шаги.

**Что снять.**

1. Карта 25 клеток на HEAD целиком, с величинами скачка по каждой клетке, а не
   только «красная/зелёная». Положить в этот документ таблицей.
2. Отпечаток `scripts/golden.mjs` на HEAD.
3. Цена третьего паруса: шаг физики с генакером и без, десктоп и телефонный
   бюджет. В `docs/perf.md` такого замера нет, а обязательство §I.5 плана
   держать бюджет открыто. Ожидание (оценка): матрица 54×54 против 36×36 и
   21 нить против 14, то есть примерно ×1.5…2 к 0.86 мс.
4. Развёртка по шагу интегрирования 1/60…1/10 на клетке 135°/5.5 (та, что была
   автоколебательной): базовые Γ_max по шагам. Нужна шагу 2.

**Ворота.** Нет. Это замер.

**Цена.** Час.

#### Замер (2026-09-11, HEAD `cc86ae2`, рабочее дерево чистое)

Карта 25 клеток, ветер 6 м/с, окно 10…25 с. Красной клетку красит Γ > 300 или
скачок тяги за шаг > 5 %.

|  курс |  шкот | Γ_max | скачок | предохр. | ход, уз |
|---:|---:|---:|---:|---:|---:|
| 120° | 3.5 | 1.69e+1 | 0.00 % | 0 | 7.47 |
| 120° | 4.5 | 7.33e+1 | 0.00 % | 5 | 8.45 |
| 120° | 5.5 | 5.80e+1 | **4.81 %** | 5 | 7.82 |
| 120° | 6.5 | 1.08e+1 | 0.24 % | 0 | 6.75 |
| 120° | 7.5 | **7.97e+2** | **17.46 %** | 4 | 5.47 |
| 135° | 3.5 | 1.11e+1 | 0.00 % | 0 | 6.13 |
| 135° | 4.5 | 4.78e+1 | 3.43 % | 1 | 7.39 |
| 135° | 5.5 | 4.31e+1 | 3.92 % | 5 | 7.90 |
| 135° | 6.5 | 2.13e+1 | 0.00 % | 0 | 7.83 |
| 135° | 7.5 | 1.15e+1 | 0.00 % | 0 | 6.53 |
| 150° | 3.5 | 4.43e+0 | 0.00 % | 0 | 5.36 |
| 150° | 4.5 | 4.65e+0 | 0.00 % | 0 | 5.67 |
| 150° | 5.5 | 6.75e+0 | 0.00 % | 0 | 6.00 |
| 150° | 6.5 | 1.60e+1 | 0.17 % | 0 | 6.34 |
| 150° | 7.5 | 1.55e+1 | 0.01 % | 0 | 6.54 |
| 165° | 3.5 | 5.38e+0 | 0.01 % | 0 | 4.97 |
| 165° | 4.5 | 3.04e+0 | 0.01 % | 0 | 5.16 |
| 165° | 5.5 | 1.13e+0 | 0.02 % | 0 | 5.29 |
| 165° | 6.5 | 2.80e+0 | 0.01 % | 0 | 5.41 |
| 165° | 7.5 | 5.89e+0 | 0.03 % | 0 | 5.57 |
| 180° | 3.5 | 4.96e+0 | 0.00 % | 0 | 4.86 |
| 180° | 4.5 | 5.62e+0 | 0.00 % | 0 | 5.03 |
| 180° | 5.5 | 4.79e+0 | 0.00 % | 0 | 5.15 |
| 180° | 6.5 | 2.96e+0 | 0.00 % | 0 | 5.22 |
| 180° | 7.5 | 2.34e+0 | 0.54 % | 0 | 5.24 |

Красная одна, 120°/7.5, как и заявлено базовой линией. Но таблица показывает
то, чего «красная/зелёная» не показывала: **120°/5.5 стоит на 4.81 % при
пороге 5 %**, то есть клетка зелёная с запасом в два процента порога. Считать
её здоровой нельзя, и правку, которая сдвинет её на полпроцента вверх, карта
объявит регрессом по делу.

Вся больная область — левая верхняя четверть карты: 120…135° при шкоте
4.5…5.5 и угол 120°/7.5. Глубже 150° карта чистая целиком, предохранитель
молчит везде.

Развёртка по шагу интегрирования, клетка 135°/5.5:

| шаг | Γ_max | скачок | предохр. | ход, уз |
|---:|---:|---:|---:|---:|
| 1/10 | 4.15e+1 | 9.28 % | 3 | 8.12 |
| 1/15 | 4.90e+1 | 6.61 % | 3 | 8.10 |
| 1/20 | 5.14e+1 | 5.19 % | 4 | 7.99 |
| 1/30 | 4.31e+1 | 3.92 % | 5 | 7.90 |
| 1/40 | 4.52e+1 | 4.92 % | 6 | 8.10 |
| 1/60 | 3.93e+1 | 4.64 % | 8 | 8.01 |

Здесь Γ не растёт: 39…51 по всему диапазону, без порядка. Зато РАСТЁТ ЧИСЛО
СРАБАТЫВАНИЙ предохранителя, ровно вдвое от 1/10 к 1/60 при вдвое меньшей
длительности каждого шага, — то есть по времени втрое чаще.

И то же на шкоте 6.4 м, TWA 140° (полоса гашения из `docs/wake.md`):

| шаг | Γ_max | скачок | предохр. | ход, уз |
|---:|---:|---:|---:|---:|
| 1/10 | 2.47e+1 | 7.45 % | 0 | 7.26 |
| 1/15 | 2.49e+1 | 0.00 % | 0 | 7.27 |
| 1/20 | 2.50e+1 | 0.00 % | 0 | 7.27 |
| 1/30 | 2.50e+1 | 0.00 % | 0 | 7.25 |
| **1/40** | **1.21e+2** | **23.49 %** | 7 | 7.22 |
| **1/60** | **2.10e+2** | **33.57 %** | 8 | 7.08 |

**Вот предсказание §5.12, снятое числом.** До 1/30 решение стоит как вкопанное
(Γ 24.7…25.0, скачок ноль), а с 1/40 разваливается и дальше только хуже: Γ
впятеро, к 1/60 в восемь с половиной раз. Мельчение шага НЕ сходится, а
расходится, и порог лежит между 1/30 и 1/40 — то есть рабочий шаг симулятора
стоит вплотную к краю. Это и есть свидетель шага 2: после правки эта колонка
обязана СХОДИТЬСЯ.

Цена третьего паруса (Node, MacBook, лодка держит TWA 140° рулевым контуром,
медиана по двенадцати пятисекундным окнам после прогрева):

| | без генакера | с генакером | |
|---|---:|---:|---:|
| деление 1 | 0.831 мс | 2.291 мс | ×2.76 |
| деление 3 | 0.533 мс | 1.390 мс | ×2.61 |

Полосок 12 → 18, матрица 36×36 → 54×54, нитей пелены 14 → 21.

**Ожидание плана (×1.5…2) не подтвердилось: третий парус стоит ×2.6.** Причина
видна из чисел: Био — Савар пелены на саму себя — 69 % шага (`docs/perf.md`
§6), нитей стало в полтора раза больше, а взаимодействие между ними квадратично,
то есть ×2.25 только на пелене. На телефоне 10.4 мс физики из 18 мс кадра
превращаются в оценочные 27 мс, то есть с генакером мобильный бюджет не сходится
уже сейчас. Условие шага 7 («не выше ×1.5 от шага 0») при таком фоне
недостижимо, и шаг 9 из условного становится вероятным.

Отпечаток `node scripts/golden.mjs` снят и лежит вне репозитория; сверка идёт
`node scripts/golden.mjs | diff <снимок> -`.

---

### Шаг 1. Сингулярность нулевого угла: непрерывный знак пуза

**Что чинится.** §4.13.6: на верхней полоске генакера угол проходит через ноль,
`camSign` переключается, рождается одношаговый выброс Γ = 797 при фоновом 21.6,
предохранитель гасит весь парус. Тот же корень у излома `luffFactor` (§4.13.4).

**Правка.** Пузо решётки обязано проходить через ноль непрерывно. Знак и
величина берутся не от `sign(alpha)`, а от гладкой функции угла: `cam_eff =
cam · s(alpha)`, где `s` нечётная, `s(±∞) = ±1`, с шириной перехода порядка
`LUFF_RANGE` = 4° (та же ширина, что у заполаскивания, чтобы не заводить второй
масштаб). Прямой кандидат: `s = tanh(alpha / LUFF_RANGE)`. Мягкое сечение на
нуле угла пуза действительно не держит, так что физика та же, что у
`luffFactor`, только без излома.

Проверить, что `camS` (сплющивание на предпоследнем проходе NLIN) и `g.fill`
после правки не образуют новой петли: по записи, как в §4.13.4, `camS` и δ по
шагам на клетке 120°/7.5.

**Свидетель.** Клетка 120°/7.5 карты: выброс Γ на переходе через ноль исчезает
(Γ_max за окно < 50 вместо 797), скачок тяги < 5 %. Карта 25/25 зелёная.

**Ворота.** Отпечаток на бейдевинде и поляре 45…120° побайтово: там углы через
ноль не ходят. Если поехал, значит `s` задета в рабочем диапазоне, и ширину
перехода надо не расширять, а искать, где она просочилась. `sailcoeffs`,
`wind`, `wake` регрессионные зелёные.

**При красном.** Если клетка не гаснет, снять, на каком именно шаге выброс:
через `camSign` или через `membraneCamber` при вырожденной нагрузке (там при
`sYp2 < 1e-12` возвращается пузо из запаса длины со знаком +1, и это второй
переключатель). Записать в §4.13.7 плана.

**Цена.** День. После зелёной карты включить `gennaker` в `FAST` Makefile
(это условие из шапки самого стенда).


#### Сделано (2026-09-12)

**Правка.** `camberSign(alpha)` в [membrane.js](../sim/membrane.js), два места
применения в [aero.js](../sim/aero.js): геометрия панелей (`camSign`) и вычет
угла нулевой подъёмной силы в `aEff`. Дуга — smoothstep, а не предложенный
планом tanh, и это не вкусовщина: smoothstep выходит на ±1 ТОЧНО при
|alpha| = LUFF_RANGE, тогда как tanh подъедал бы пузо и на десяти градусах, где
чинить нечего. Ширина та же, что у заполаскивания, как план и требовал.

**Свидетель — взят.** Клетка 120°/7.5: Γ_max **797 → 12.0** (план требовал
< 50), скачок тяги **17.46 % → 0.01 %**, предохранитель 4 → 0, ход 5.47 → 5.42
уз. **Карта 25/25 зелёная.** Двадцать клеток из двадцати пяти не изменились ни
в одном разряде; шевельнулись только 120°/6.5 (скачок 0.24 % → 0.00 %) и
120°/5.5 (Γ 58.0 → 58.7, скачок те же 4.81 %). Развёртка по шкоту при TWA 140°
совпала с базовой целиком.

`make test` зелёный (593 проверки), `upwind`, `wake`, `wind`, `sailcoeffs`
зелёные. `gennaker` включён в `FAST`, шапка стенда переписана.

#### Ворота не взяты, и это находка, а не поблажка

План требовал побайтового совпадения отпечатка на бейдевинде, обосновывая это
тем, что «там углы через ноль не ходят». **Отпечаток разошёлся во всех пяти
сценариях.** По правилу «порог не двигают под ответ» посылка ворот проверена
замером, прежде чем ворота трогать.

Счётчик попаданий в полосу |alpha| < 4° за сорокапятисекундный прогон, по
вызовам `camberSign`:

| сценарий отпечатка | в полосе | из них РОВНО на нуле |
|---|---:|---:|
| бейдевинд, экипаж на борту | 4.79 % | 0.72 % |
| парусность и твист на ходу | 15.06 % | 0.82 % |
| левентик, руль брошен | 90.06 % | 33.62 % |
| полный курс с перекладкой | 61.97 % | **59.75 %** |

Посылка ворот верна ровно наполовину. В УСТАНОВИВШЕМСЯ режиме геометрический
угол в полосу действительно не заходит: на бейдевинде, галфвинде и бакштаге
после десяти секунд — 0.0 % живой парусности. Но столбец «ровно на нуле»
показывает другое, и вот оно:

**У сваленного по потоку паруса угол атаки равен нулю не приблизительно, а
тождественно.** При `sheet >= awa` вынос равен awa, хорда встаёт ровно по
потоку, и `alpha = wrapPi(theta - chordDir)` обращается в ноль точно, а не
почти. Со старым `Math.sign(alpha || 1)` такая полоска получала знак **+1** и
строилась с ПОЛНЫМ проектным пузом — то есть решётка выдавала подъёмную силу
пуза полощущему парусу. На полном курсе с перекладкой так стояли шесть вызовов
из десяти, в левентике — треть.

Силовая половина модели с этим давно не соглашалась: `luffFactor` там даёт
ноль, и пузо в силах пропадало. Расходились ГЕОМЕТРИЯ решётки и СИЛЫ — ровно то
расхождение, которое правило «одна реализация одной вещи» и запрещает. Правка
его закрывает, и потому обязана двигать отпечаток: побайтовое совпадение
означало бы, что она не сработала.

**Ворота заменены на такие** (и это ослабление названо вместе с обоснованием,
как требует правило):

- в установившемся режиме геометрический угол в полосу перехода не заходит:
  0.0 % живой парусности на бейдевинде 8 м/с, галфвинде 7 м/с и бакштаге 6 м/с
  (замерено);
- за полосой правка не меняет ни одного разряда: 20 клеток карты из 25
  совпали побайтово, развёртка по шкоту при TWA 140° — целиком;
- поляра бейдевинда двигается в пределах 0.06 узла, `upwind.test.mjs` зелёный.

Что сдвинулось на бейдевинде (`upwind.test.mjs`, до → после):

| | было | стало |
|---|---:|---:|
| лучший VMG, 6 м/с | 3.46 уз | 3.45 уз |
| лавировочный угол | 93° | 94° |
| ход на TWA 40° | 5.05 уз | 5.03 уз |
| ход в ДП, 11 м/с | 4.43 уз | 4.49 уз |
| ход в ДП, 14 м/с | 5.54 уз | 5.60 уз |
| наибольший крен в левентике | 67° | 66° |

Наибольшее расхождение отпечатка по ходу — 0.076 узла (левентик, где лодка
дрейфует на узле), по крену 1.4°, по тяге 16 Н из восьмисот.

#### Что осталось незакрытым

Второй переключатель, названный в «При красном», НЕ тронут: `membraneCamber`
при вырожденной нагрузке (`sYp2 < 1e-12`) возвращает знак +1. Карта позеленела
без него, поэтому трогать его сейчас значило бы менять то, что не меряется.
Записано здесь, чтобы не искать заново.

---

### Шаг 2. Неявность ближнего следа: кольцо длиной с панель

**Что чинится.** Установленный, но не закрытый корень разноса (`docs/wake.md`,
«Полоса 5.8…6.6 разобрана»): усиление отрицательной связи со вторым рядом пелены
|g| до 2.19 при явном учёте, знакочередующийся рост Γ, и чем мельче шаг, тем
хуже (1/60 разносит даже спокойный шкот 6.8 м). Там же назван прямой ход:
«сажать кольцо не каждый шаг, а раз в несколько, длиной с панель; тогда весь
ближний след попадёт в матрицу». Это классическое правило UVLM, и оно у нас
нарушено вчетверо.

**Правка.** Свежее кольцо растёт, пока его длина меньше хорды панели полоски
(у генакера панель 1.39 м, у грота 0.8 м, у топа меньше), и только потом
замораживается и сходит. Пока кольцо растёт, оно целиком стоит в матрице
(`nearWake`), то есть неявно; его циркуляция равна текущей Γ полоски. Это НЕ
запаздывание гашения и не недорелаксация: связь та же мгновенная, меняется
только то, какая часть следа решается вместе с Γ.

Два подводных камня, которые надо проверить, а не предположить:

- у разных полосок разная хорда панели, значит кольца сходят не синхронно;
  рёбра между нитями соседних полосок (`w.ring[f]`) должны это пережить;
- запаздывание по Вагнеру считается пеленой (`tests/wake.test.mjs`, 0.66 на
  второй полухорде против 0.67). Более длинное свежее кольцо сдвинет этот
  отсчёт; допуск стенда обязан остаться, а не быть расширен.

**Свидетель.** Развёртка по шагу 1/60…1/10 из шага 0 на клетке 135°/5.5 и на
шкоте 5.8…6.6 м при TWA 140°: Γ_max обязан СХОДИТЬСЯ при мельчении шага, а не
расти. Усиление g (мерка из §5.12, возмущение первых K рядов) на шкоте 6.4 м
падает с −2.19 ниже единицы по модулю.

**Ворота.** Вагнер в `wake.test.mjs` в прежнем допуске. Поляра 45…120° в
пределах 1 % (пелена меняется, побайтового совпадения ждать нельзя, но
установившийся ход на присоединённых курсах меняться не должен). `wing.test`
(чистое крыло) зелёный.

**При красном.** Если Γ_max всё равно растёт при мельчении шага, значит петля
не во втором ряду, и диагноз §5.12 неполный; записать развёртку и остановиться,
шаги 3 и 5 от этого не зависят.

**Цена.** Два-три дня. Самый рискованный из первых трёх, но с самым точным
предсказанием того, что должен показать замер.

#### Красный (2026-09-12): правка отвергнута, диагноз заменён

Сделано ровно то, что предписано: `FreeWake.shedEvery` = c_панели/(V·Δt), свежее
кольцо растёт в матрице, хорда берётся средняя по сбрасывающим полоскам
(кольцо одно на пелену, ряды обязаны оставаться синхронными по нитям). Вышло 5
шагов при 1/30.

**Свидетель не взят: развёртка по шагу не сошлась.** Более того, развёртка по
ДЛИНЕ КОЛЬЦА × по шагу показала, что длина кольца не влияет ни на что: на
1/10…1/30 Γ = 24…25 при любой длине от 1 до 12 шагов, на 1/40 и мельче разносит
при любой, и разброс между длинами (48…730 на 1/40) беспорядочен.

Отвергнута по четырём замерам (числа — в `docs/wake.md`, «Отвергнуто: кольцо
пелены длиной с панель»): свидетеля не взяла; карта генакера 25/25 → 3 красных
(120°/6.5 скачок 0.24 → 17 %); стенд пелены покраснел на проверке «сошедшие
кольца шкотом не переписываются»; Вагнер на второй полухорде ушёл с 0.66 на 0.80
против точных 0.67. Откачено, карта снова 25/25.

**Зато найдена настоящая причина, и это ровно та запись, которую §4 плана назвал
самой ценной.** Контрольный опыт на той же клетке:

| постановка | 1/30 | 1/40 | 1/60 | 1/90 |
|---|---|---|---|---|
| пелена в силах | 2.5e+1/0 % | 1.2e+2/23 % | 2.1e+2/34 % | 3.8e+2/48 % |
| БЕЗ пелены в силах | 2.2e+1/0 % | 2.2e+1/0 % | 2.2e+1/0 % | 2.2e+1/0 % |
| без генакера | 6.4e+0/0 % | 6.4e+0/0 % | 6.4e+0/0 % | 6.4e+0/0 % |
| руль заперт | 2.6e+1/2 % | 6.6e+1/10 % | 6.5e+1/17 % | 4.6e+2/44 % |

Без пелены в силах Γ = 22 на любом шаге до 1/90 — то есть зависимость от шага
живёт целиком в пелене, но не в длине её колец. А выключение ВЫТАЛКИВАНИЯ УЗЛОВ
ИЗ ПАРУСА (`keepWakeOut`) разнос снимает: 25 / 27 / 55 / 30 вместо 25 / 120 /
210 / 380.

Механизм называется в одну строку: выталкивание — позиционная поправка на шаг,
её величина (перенос до 0.093 м) от Δt не зависит, значит как скорость она равна
переносу, делённому на Δt, и при мельчении шага растёт как 1/Δt. Модель получает
тем более резкий пинок узлам у полотна, чем мельче шаг.

Побочно померено и записано: постоянные времени модели (`TRANSFER` = 15 шагов,
`FUSE_HOLD` = 30, `FUSE_HOLD_MAX` = 240) заданы в ШАГАХ, то есть при 1/60 вдвое
короче по времени. Перевод в секунды помогает вдвое (Γ 210 → 140 на 1/60), но
порога не двигает. Это вклад, а не причина.

**Что из этого следует для плана.** Шаг 2 в нынешней постановке закрыт как
отвергнутый. Вместо него — новый пункт: выталкивание узлов обязано быть
скоростью, а не переносом за шаг.

#### Шаг 2а. Долг оплачен: запрет лететь в парус — скоростью (2026-09-12)

`Rig.sailBlock` вместо `keepWakeOut`: запрет стоит ДО сноса и действует В МЕРУ
ГЛУБИНЫ захода, выход наружу — тоже. Числа и разбор в `docs/wake.md`
(«Сделано: запрет лететь в парус»). Коротко:

| | было | стало |
|---|---:|---:|
| Γ на 1/40 (шкот 6.4 м, TWA 140°) | 1.2e+2 | **2.4e+1** |
| Γ на 1/60 | 2.1e+2 | **3.5e+1** |
| Γ на 1/90 | 3.8e+2 | 1.4e+2 |
| Вагнер, вторая полухорда (точно 0.67) | 0.79 | **0.77** |
| запаздывание на перекладке шкота | 0.75 | 0.63 |
| ближе всех узел подошёл к полотну | 0.093 м переноса | 0.002 м из 0.20 |
| цена шага с генакером, деление 3 | 1.390 мс | 1.384 мс |
| бейдевинд, лучший VMG | 3.45 уз | 3.44 уз |

Разнос на 1/40 — то есть на шаге сразу за рабочим — снят полностью. `make test`,
`upwind`, `wing`, `wake`, `wind` зелёные.

**И заодно вскрылось, что карта мерялась одним прогоном.** Клетка 135°/4.5
хаотична: от тысячной доли узла в начальном ходе скачок ходит через порог в обе
стороны (на HEAD 2.9…6.6 %, размах 3.6 п.п.), тогда как остальные двадцать
четыре при том же возмущении стоят побитово. То есть «25/25» из шага 1 держалось
на том, какое число стояло начальным ходом. Карта теперь меряется с двух
начальных ходов с худшим из двух, и честный итог — **24/25 и на HEAD, и после
правки**, красная одна и та же клетка.

Цена правки по этой клетке названа прямо: размах вырос до 16.7 п.п.
(20.4, 3.7, 19.6, 4.1, 11.5 %). Источник скачка там не в пелене — её зависимость
от шага снята, — а в переключении постановки полоски: предохранитель на этой
клетке срабатывает. Это ровно то, что чинит шаг 3.

Батарея генакера из `FAST` убрана обратно: набор обязан оставаться зелёным.

---

### Шаг 3. Сорванная полоска: предписанная пелена вместо вынимания

**Что чинится.** Нарушение Кельвина у вынутой полоски (несёт предписанную Γ,
не сбрасывает ничего, `wakeShed = 0`) и сам переключатель постановки, который
литература не использует. В VSM полоска всегда решается, а её след предписан
подковой вдоль потока; это и есть второй решатель проекта «без свободной
пелены, который не разносится никогда» (§5.12, гибридный режим).

**Правка.** Полоска с доверием ниже `LAT_OUT` не вынимается, а переводится на
предписанную пелену: её сходящие вихри становятся полубесконечными лучами вдоль
местного потока и входят в МАТРИЦУ (неявно, как у решателя без пелены), а
свободная пелена от неё не сходит. Уравнение непротекания и поправка δ по
секущей остаются, Γ по-прежнему тянется к поляре сечения. Гистерезис
`LAT_OUT`/`LAT_IN` и предохранитель остаются как есть: переключается не
постановка, а модель следа одной полоски.

Что это меняет по сравнению с отвергнутыми версиями: там либо оставляли
свободную пелену и гасили сцепление множителем (усилитель), либо убирали
полоску из системы (нет скоса на соседей). Здесь полоска в системе, скос на
соседей наводит, след у неё есть, и он неявный.

Сила по-прежнему считается полярой сечения от эффективного угла; наклон по
скосу от пелены для такой полоски равен нулю, как и сейчас (за срывом поляра
уже содержит сопротивление плохо обтекаемого тела).

**Свидетель.** Карта 25/25 без предохранителя вовсе: `fuseTrips = 0` на всех
клетках. Стенд линий тока §4.13.4 (поле на TWA 165° живое, отклонение
скорости не ноль). Число переключений полоски за прогон падает против шага 0.

**Ворота.** Отпечаток на бейдевинде побайтово (там доверие 1, ветка не
включается). Поляра 45…120° в пределах 1 %. Инвариант акватории: попутное
течение СБАВЛЯЕТ ход через воду (тот, что сломала двенадцатая версия). Ход при
TWA 90° и ветре 8 м/с в пределах 0.05 узла.

**При красном.** Если у предписанной пелены полоска на глубоких курсах
получает Γ выше потолка сечения, значит секущей δ не хватает и на подкове;
тогда добавить упор Γ ≤ `CEIL_MARGIN`·потолок прямо в уравнение (одно
ограничение, не множитель) и перемерить. Если инвариант акватории
переворачивается, откатить и записать: эта ветка не даёт затенения гротом, а
затенение и было причиной знака.

**Цена.** Два дня. Зависит от шага 2 только тем, что оба трогают `nearWake`;
делать после него.

#### Красный (2026-09-12): идея верна, предписанный луч — нет

Правка сделана как написано: `latMask`/`latPre` сняты, полоска с низким доверием
остаётся в уравнении непротекания, свободная пелена от неё не сходит, а прямые
лучи (`tailWeight` и так даёт ей единицу) стоят в матрице.

**Половина свидетеля взята сразу:**

| | было | стало |
|---|---:|---:|
| переключений полоски по карте | 191 | **79** |
| скачок на клетке 135°/4.5 (той самой, хаотичной) | 20.4 % | **0.0 %** |
| поле решётки на TWA 165°, пик | 1.04 м/с | 5.73 м/с |

Стенд линий тока §4.13.4 закрывается по построению: полоска несёт решаемую
циркуляцию, поле живое во всех 396 пробных точках.

**И хоронит правку ровно то же самое.** Γ на глубоких курсах растёт монотонно с
уходом ветра в корму (шкот 3.5 м): 54, 97, 210, 240, **500** при TWA 120…180°.

Причина не новая и записана в `docs/wake.md` задолго до этого плана: на полных
курсах поток идёт от кормы, вниз по потоку — это ВПЕРЁД, и прямой луч уходит от
задней кромки не в след, а в собственный риг. Там же сказано прямо: «Прямой луч —
это ровно та модель, от которой пелена и уходила, и возвращать её в самом
неудобном для неё месте нечего». Шаг 3 предлагает вернуть её именно туда:
сорванные полоски живут как раз на полных курсах.

Почему на HEAD этих лучей хватает: они там ТОЖЕ есть, просто несут предписанную
из поляры, то есть малую циркуляцию. Стоит сделать её решаемой — и лучи,
направленные в свой же риг, её разгоняют.

**Упор по циркуляции, предписанный планом на этот случай, померен в четырёх
постановках; ни одна не проходит ворота** (числа — в `docs/wake.md`, «Отвергнуто:
предписанная пелена у сорванной полоски»). Самая близкая — упор по потолку
сечения на всех полосках: карта становится идеальной (0 красных из 25,
предохранитель молчит везде, переключений 18), но ход на галфвинде при 8 м/с
прыгает с 6.58 до 8.47 узла, то есть на 29 % при воротах в 0.05 узла. Подбирать
упору место дальше значило бы подгонять его под ответ. Откачено целиком.

**Что менять в плане.** Идея VSM («полоска всегда в уравнении») верна и на карте
это показала. Неверна ПРЕДПИСАННАЯ ПЕЛЕНА ПРЯМЫМ ЛУЧОМ. Чтобы шаг 3 стал
возможен, подкова должна быть загнута по направлению свободного листа соседей, а
не пущена прямо, — и это уже работа, соразмерная шагу 7, а не два дня.

Побочно установлено и стоит отдельного внимания: **упор по потолку сечения,
приложенный ко всем полоскам, меняет ход на галфвинде на 29 %.** Это значит, что
решётка сегодня на присоединённых курсах работает ВЫШЕ потолка сечения, и вся
настройка лавировки на этом стоит. Правда это или нет — нынешними стендами не
решается; это вопрос ровно к шагу 4.

---

### Шаг 4. Эталон по силам целого паруса

**Что чинится.** Единственная слепая зона, которую не закрывает ни один
стенд: КАКУЮ силу даёт генакер по курсу. Все стенды меряют устойчивость,
непрерывность и согласие сечения с решёткой, а не величину.

**Правка.** Завести `data/sail/` рядом с `data/section/` и положить туда
оцифрованные трубные кривые асимметричного спинакера: C_x (тяга) и C_y
(боковая) по AWA при лучшем триме, с источником, числом Рейнольдса и способом
оцифровки в самом JSON, как сделано у Уоллиса и Милгрэма. Кандидаты:
Viola & Flay (труба Окленда, спинакер + грот, AWA 40…120°+), Graf/Renzsch
(валидационный случай для FSI: 6-компонентные силы и полётные формы по AWA и
триму). Второй источник обязателен: один ряд свидетелем не считается.

Чужой парус не наш. Сравнение идёт не по абсолюту, а по ФОРМЕ кривой:
отношение C_x к площади по AWA, положение максимума тяги, спад к 180°, и
отношение C_y/C_x. Полоса допуска назначается заранее, до первого прогона:
±15 % по C_x в максимуме и ±10° по положению максимума (оценка; литература
даёт разброс между парусами такого порядка).

Стенд: `tests/sailforce.test.mjs`, генакер с гротом на ветре 6 м/с, курс
110…180° через 10°, шкот перебирается до максимума тяги (как в трубе трим
подбирают по тяге), выход: таблица C_x, C_y и сравнение с эталоном.

**Ворота.** Нет: первый прогон записывается как есть, красный или зелёный. Это
базовая линия для шагов 5 и 6, и двигать её потом нельзя.

**Цена.** День на данные (оцифровка по рисункам, если таблиц в статьях нет),
полдня на стенд. Не зависит от шагов 1…3, можно делать параллельно.

#### Сделано (2026-09-12). Первый прогон — красный, и это ценнее зелёного

**Данные.** `data/sail/orc_vpp_2023.json`: табличные коэффициенты ORC VPP 2023 —
грот (табл. 5.1), асимметричный спинакер на ДП (5.7, наш случай: галс на
бушприте по ДП), он же на пуле (5.8) и симметричный (5.6). Не оцифровка с
рисунка: числа извлечены из PDF документа текстом, то есть ровно те, что
напечатаны. Родословная — Hazen (1980) → IMS VPP 2003 → правки по трубным
испытаниям, последняя по одиночным парусам в 2016.

**Второй источник НЕ получен, и это записано в самом файле.** Viola & Flay 2009
(IJSCT 151 B2) — открытый файл на research.ed.ac.uk закрыт Cloudflare; Campbell
2014 и Viola & Flay 2014 — за платной стеной, и в обоих величины даны рисунками.
Пока второго ряда нет, вердикт стенда ПРЕДВАРИТЕЛЬНЫЙ: он говорит «разошлись с
ORC», а не «модель неверна». Из обзоров без чисел известно, что кривые разных
труб за шестнадцать лет сходятся в пределах 10 %, а максимум подъёмной силы
приходится на кажущийся угол 50…70° — форме нашей полосы допуска это не
противоречит.

**Стенд.** `tests/sailforce.test.mjs`, генакер с гротом, ветер 6 м/с, TWA
110…180° через 10°, шкот перебирается до максимума тяги (как трим подбирают в
трубе). Прогон идёт через пул потоков, новым сценарием `sailForce` в
`tests/lib/runs.mjs`. Включён в `make slow`.

Эталон собран ровно по документу: взвешенная по площадям сумма коэффициентов
грота и спинакера, затенение для шлюпа тождественно единица, индуктивное
сопротивление по эффективной высоте рига, и дальше
C_x = C_L·sin β − (C_D0 + C_Di)·cos β, C_y = C_L·cos β + (C_D0 + C_Di)·sin β.

**ПЕРВЫЙ ПРОГОН, регрессионный режим, записан как есть:**

| курс | шкот | AWA | ход, уз | крен | C_x | C_y | эталон C_x | эталон C_y |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 110° | 3.5 | 58° | 9.63 | 7° | 1.014 | −0.910 | 0.694 | 1.028 |
| 120° | 4.7 | 71° | 8.32 | 5° | 1.116 | −0.534 | 0.908 | 0.948 |
| 130° | 4.7 | 77° | 8.54 | 5° | 1.606 | −0.683 | 0.987 | 0.873 |
| 140° | 7.0 | 99° | 7.02 | 8° | 1.644 | −0.455 | 1.158 | 0.566 |
| 150° | 7.0 | 116° | 6.60 | 9° | **2.056** | −0.426 | 1.111 | 0.349 |
| 160° | 8.2 | 137° | 6.05 | 12° | **2.058** | 0.118 | 0.808 | 0.201 |
| 170° | 8.2 | 160° | 5.46 | 12° | 1.516 | 0.235 | 0.693 | 0.127 |
| 180° | 7.0 | 178° | 5.24 | 9° | 1.266 | −0.329 | 0.662 | 0.049 |

(знак C_y у модели говорит, с какого борта ветер; эталон ORC по построению
положителен, поэтому сравнение идёт по модулю.)

Три проверки из четырёх красные:

- **максимум тяги +78 %**: C_x = 2.058 против 1.158 при полосе ±15 %;
- **положение максимума +38°**: AWA 137° против 99° при полосе ±10°;
- отношение боковой к тяге расходится на 0.58 при пороге 0.5 (худшее на 110°);
- **спад к фордевинду сошёлся**: 0.62 от максимума против 0.57 у эталона.

**Независимое подтверждение, которое стоит дороже самого сравнения.** Ход в
таблице: 9.63 узла при TWA 110° и истинном ветре 11.7 узла. Длина по ватерлинии
5.47 м, то есть корпусная скорость 5.7 узла, — модель идёт **1.7 корпусной** под
генакером в свежий бриз. Владелец лодки говорит, что не глиссировал ни разу, а
14 узлов на реке редкость. То есть тяга завышена не «по чужому эталону», а по
тому, как лодка ходит на воде.

**Что это значит для остальных шагов.**

1. Побочная находка шага 3 получила подтверждение с другой стороны: там упор по
   потолку сечения на всех полосках разгонял галфвинд на 29 %, и было непонятно,
   в какую сторону это ошибка. Теперь понятно — модель и без упора даёт слишком
   много, значит упор двигал её ДАЛЬШЕ ОТ правды.
2. Шаг 5 (вихрь передней кромки, добавка Полхамуса) ПРИБАВЛЯЕТ подъёмной силы за
   срывом. Делать его сейчас нельзя: модель и так завышает, и добавка сделает
   хуже. Порядок в плане меняется — сперва разобраться, откуда лишнее.
3. Смещение максимума тяги на 38° вглубь — самостоятельный признак. Он говорит
   не «везде больше на столько-то», а «кривая не той формы»: на острых углах
   модель ближе к эталону (+46 % на 58°), на глубоких вдвое-втрое. Это указывает
   на продолжение поляры за срывом, а не на общий множитель.

**Ворот у шага не было, и они не назначаются задним числом.** Полоса ±15 % и
±10° назначена до прогона и остаётся; красный записан.

### Откуда лишняя тяга: разобрано (2026-09-12)

Вопрос, поставленный первым же прогоном шага 4. Разбор идёт сверху вниз: сперва
кто именно лишний, потом где в цепочке это возникает.

#### 1. Лишний — генакер, грот сходится

Разложение по парусам на TWA 150° (AWA 116°, ветер 6 м/с, шкот 7.0 м),
коэффициенты по СВОЕЙ площади каждого паруса:

| | модель | ORC на том же AWA | |
|---|---:|---:|---|
| грот | C_x = 1.320 | 1.267 | **+4 %** |
| генакер | C_x = 2.483 | ≈ 0.94 | **+164 %** |

То есть общий промах в +78 % — это не «модель вообще завышает», а **генакер
вдвое с лишним**. Грот при этом попадает в эталон.

#### 2. Поляра сечения не виновата

Пузо полосок генакера 0.164…0.197, то есть ряд Милгрэма 18 %. Что даёт модель:

| α | 10° | 20° | 24° | 33° | 50° | 90° |
|---|---:|---:|---:|---:|---:|---:|
| cl | 1.850 | 2.400 | 2.420 | 2.152 | 1.456 | 0.000 |
| C_N | 1.842 | 2.429 | **2.527** | 2.405 | 2.013 | 1.280 |

На измеренном крае (24°) C_N = 2.53 — это ровно Милгрэм для 18 % пуза; дальше
кривая гладко сходит к пластине (C_N = 1.28 при 90°). Двумерное сечение
воспроизводится верно, и трогать его незачем.

#### 3. Трёхмерного снижения НЕТ ВОВСЕ — вот это и есть ответ

Полная сила генакера, отнесённая к его площади, против двумерной поляры его же
сечений на их же углах:

| курс | AWA | C_N паруса целиком | C_N сечений (2D) | отношение | ORC |
|---:|---:|---:|---:|---:|---:|
| 130° | 78° | 2.205 | 2.479 | **0.89** | 1.02 |
| 150° | 116° | 2.551 | 2.410 | **1.06** | 1.02 |
| 170° | 160° | 1.745 | 1.820 | **0.96** | 0.72 |

**Парус конечного размаха даёт ровно столько же, сколько бесконечный, а на
одном курсе и больше.** Классическая теория для этого паруса (удлинение 2.74,
с зеркалом от воды вдвое больше) дала бы 0.6…0.75, эталон требует 0.42. У нас
1.00.

#### 4. Почему поправка не работает: она действует через УГОЛ, а поляра плоская

Цепочка силы такая: решётка даёт Γ → из Γ обращением тонкой теории получается
эффективный угол → по нему берётся cl из двумерной поляры → это и есть сила.
Трёхмерность входит в неё ровно одним слагаемым — скосом, вычитаемым из угла.

Замер скоса на TWA 150° (сырой, до гашения; гашение `fadeOf`; учтённый):

    полоска     12     13     14     15     16     17
    сырой     8.4°   1.0°   0.3°   5.1°   7.3°  10.1°
    гашение   0.57   0.23   0.11   0.08   0.09   0.13
    учтённый  4.8°   0.2°   0.0°   0.4°   0.6°   1.3°

И даже если гашение снять, ничего не выйдет — по двум разным причинам, и обе
померены.

**Первая: поляра у максимума плоская.** C_N при 19° = 2.43, при 33° = 2.41.
Поправка по углу в четырнадцать градусов меняет силу на процент. У паруса,
работающего вблизи своего максимума, СИЛУ нельзя уменьшить, поправляя УГОЛ. На
TWA 130°, где гашение частично живо (0.53…1.0) и скос доходит до 8°, отношение
всё равно 0.89.

**Вторая: без гашения обращение тонкой теории даёт бессмыслицу.** Замер с
`fade = 1`:

| курс | скос генакера | C_x рига |
|---:|---|---:|
| 130° | 9…17° | 1.544 (было 1.607) |
| 150° | 45…47° | **0.054** (было 2.055) |
| 170° | 51…60° | 0.337 (было 1.513) |

Формула Γ = π·c·V·(α − α₀) верна при малых углах и присоединённом потоке; на
33…45° с оторванным она обращается в «скос» 45…60°, и парус умирает. Гашение
`fadeOf` — не ошибка, а затычка: оно выключает механизм ровно там, где механизм
перестаёт быть верным. Выключить затычку нельзя, но и оставить как есть тоже:
выключенный механизм означает двумерный парус.

#### Что из этого следует

**Дефект структурный: на полных курсах у модели нет ИСПРАВНОГО способа перейти
от двумерной поляры сечения к силе трёхмерного паруса.** Обе ветки негодны —
одна (скос через угол) не может уменьшить силу у плоского максимума поляры,
другая (обращение тонкой теории) на этих углах не определена.

Отсюда прямые следствия для плана:

- **Шаг 5 (вихрь передней кромки) отменяется, а не откладывается.** Он
  ПРИБАВЛЯЕТ подъёмной силы за срывом, то есть лечит ошибку противоположного
  знака той, что измерена.
- **Шаг 6 (замкнуть форму) от этого не спасёт**: он меняет геометрию панелей, а
  промах не в геометрии — сила уже равна двумерной.
- Нужное лечение лежит там, куда ни один шаг плана не смотрел: снижение силы
  ЦЕЛОГО паруса по его удлинению, а не по углу каждой полоски. У нас уже есть
  всё, чтобы его назначить не на глаз: удлинение считается из обвода, зеркало от
  воды в решётке уже стоит, а свидетель — стенд шага 4.
- Половина промаха объясняется этим сразу (0.65 из 0.42/1.00), вторая половина —
  разница между жёстким сечением у своего оптимума и настоящим спинакером
  (заворот передней шкаторины, твист, работающая вполсилы верхушка). Эту вторую
  половину без второго ряда трубных данных разделить нельзя, и назначать её
  множителем сейчас значило бы подгонять под ORC.

---

### Шаг 5. Вихрь передней кромки в поляре за срывом

**Что чинится.** Продолжение за ±24° нормальной силой плоской пластины не
знает про LEV, а у топа спинакера он устойчив и даёт 10…25 % секционной
подъёмной силы (Arredondo-Galeana & Viola 2018; диссертация Эдинбурга). У нас
верхняя полоска генакера и есть та, что живёт за срывом.

**Правка.** В `_deep_cl` добавка Полхамуса: `Cl_v = K_v · sin²α · cos α`,
входит с тем же гладким весом `s`, что и переход к пластине, и гаснет к 90°.
`K_v` один на ряд поляры (12 % и 18 %). Сопротивление по той же аналогии:
`Cd_v = K_v · sin³α`. Уменьшение K_v от фала к галсу (LEV сходит ниже) НЕ
вводится на этом шаге: это второй параметр, а свидетель один.

Порядок величины для первого прогона: Полхамус для дельта-крыла даёт K_v ≈ π
при малом удлинении; для сечения с LEV, дающим 10…25 % подъёмной силы при
35°, это K_v ≈ 0.5…1.2 (оценка по `Cl(35°) ≈ 2.07`). Начать с 0.8.

**Свидетель.** Стенд шага 4: максимум тяги по AWA приближается к эталону, а
не уходит от него. CFD-зонд 1б остаётся в полосе: Cl(35°, пузо 18 %) в
пределах 2.17 ± 0.25 (§13.14 `docs/cfd-validation.md`). `sailcoeffs` и
`milgram.test.py`: измеренная часть ±24° побайтово.

**Ворота.** K_v подбирается ОДИН раз по стенду шага 4 и записывается в
`polar.py` с числом и датой. Если для попадания в эталон нужен K_v > 1.5,
значит эталон и продолжение расходятся не из-за LEV, и правку не принимать:
записать в `docs/wake.md` с числами.

**Цена.** Полдня правки, полдня замера. Зависит от шага 4.

---

### Шаг 6. Замкнуть форму: мембранное пузо обратно в решётку

**Что чинится.** Решётка строится по проектному пузу, а силы считаются по
летящему; при нагрузке, не похожей на проектную (генакер на глубоких курсах,
отданный грот), две половины модели считают разную геометрию. Литература по
FSI генакера показывает, что слабая связь с релаксацией сходится без
итераций внутри шага.

**Правка.** Панели полоски строятся по `cam_lat`, которое между кадрами
догоняет мембранное пузо с недорелаксацией: `cam_lat += r · (cam_mem −
cam_lat)`, r ≈ 0.1…0.2 за шаг (оценка; постоянная времени порядка `TRANSFER`,
то есть хорда на скорость). Проектное пузо остаётся начальным значением и
значением при вырожденной нагрузке. Комментарий в [aero.js:1210](../sim/aero.js:1210)
предупреждает: «с летящим пузом поправка сама уменьшает то, к чему приводит,
и риг за три шага складывается». Поэтому поправка δ по-прежнему считается с
`camPanel`, а летящее пузо идёт ТОЛЬКО в геометрию панелей; менять обе точки
разом нельзя.

**Свидетель.** Согласие cl сечения с cl решётки в `wind.test.mjs` (сейчас
0.90…0.99) не расползается. Карта 25/25. Стенд шага 4: тяга меняется, и
запись того, в какую сторону, обязательна, даже если в допуск не попадает.

**Ворота.** Бейдевинд: ход в пределах 0.05 узла, порядок сдачи кромок на
добранном шкоте прежний (`upwind.test.mjs`). Если риг складывается, как
предсказано в комментарии, снизить r вдвое один раз; если не помогло,
откатить и записать.

**Цена.** День. Не зависит от шагов 2…5, но мерить после них, иначе не
отделить вклады.

---

### Шаг 7 (условный). Вторая пелена с передней кромки

**Когда делать.** Только если после шага 3 стенд шага 4 показывает, что
глубокие курсы (150…180°) систематически не добирают тяги против эталона, и
шаг 5 этого не закрыл. Иначе не делать: это удвоение нитей пелены при бюджете
0.86 мс на десктопе и 10.4 мс из 18 на телефоне.

**Правка.** По многопелёночному VLM для мембранных кайтов (Leuthold, TU
Delft): с подсасывающей стороны сорванной полоски сходит вторая пелена с
известной линии отрыва (передняя кромка), с ядром Ватистаса, комбинированная
свободная и фиксированная. Литература: C_L и C_D в пределах ~10 % от RANS,
слабое место у линии присоединения. Критерий схода с передней кромки: LESP
(Ramesh), у нас это `a0` из `sectionShape` или циркуляция первой панели по
хорде, нормированная на V·c; порог `LESP_crit` один на модель.

**Свидетель.** Стенд шага 4 на 150…180° и `docs/perf.md`: цена шага с
генакером не выше ×1.5 от шага 0.

**Цена.** Неделя. Отдельный план перед началом.

---

### Шаг 8 (условный). Состояние «сложился»

**Когда делать.** После того, как карта и эталон зелёные. Это про ощущение
управления, а не про поляру.

**Правка.** У генакера появляется состояние `collapsed` с гистерезисом:
складывание при `luffFrac` выше порога дольше τ_c, наполнение при угле выше
идеального дольше τ_f, τ_c втрое короче τ_f (то, что девятая версия делала для
`fill` и что было отвергнуто для ГАШЕНИЯ, здесь относится к состоянию ткани, а
не к связи с пеленой; связь остаётся мгновенной). Сложенный парус несёт только
сопротивление вымпела. Трубные данные о том, что в натуре трим держат туже, чем
в трубе, дают качественный признак: сложенный парус должен требовать подбора
шкота, а не наполняться сам.

**Свидетель.** Новый стенд: сценарий «отдал шкот до сложения, подобрал»,
время наполнения в секундах и отсутствие автоколебания сложился/наполнился на
границе. Карта 25/25 без изменений (состояние не должно включаться на
рабочих клетках).

**Цена.** Два дня, плюс мобильное управление (§4.10, не сделано) и запись
`gennakerUp`/`genSheetLen` в trace (§4.5), без которых сценарий не
воспроизводим.

---

### Шаг 9 (условный). Таблица вместо расчёта на телефоне

**Когда делать.** Если после шагов 2…3 телефонный бюджет не сходится.

**Правка.** Не нейросеть: таблица сил генакера по (AWA, шкот, ветер) снимается
с ПОЛНОЙ модели офлайн и используется на телефоне вместо третьего паруса в
решётке. Это тот же приём, что суррогаты по CFD в литературе, но эталон свой.
Условие: таблица считается тем же кодом, что и живая модель («одна реализация
одной вещи»), и стенд сверяет её с живым расчётом в узлах.

**Цена.** Три дня. Сначала замер шага 0, потом решение.

---

## 3. Порядок и остановки

    0 → 1 → 2 → 3 → (4 параллельно с 1…3) → 5 → 6 → [7] → [8] → [9]

Остановка после шага 3: карта 25/25, предохранитель молчит, развёртка по шагу
сходится. Если этого нет, дальше идти нельзя: шаги 5 и 6 меняют силы, и на
неустойчивой модели их вклад не отделить.

Остановка после шага 6: первая запись в этот документ с таблицей C_x, C_y
против эталона до и после каждого из шагов 3, 5, 6. Это и есть ответ на
вопрос «стало ли точнее», и без неё шаги 7…9 не обсуждаются.

---

## 4. Риски, названные заранее

- **Шаг 2 может показать, что диагноз §5.12 неполный.** Развёртка по шагу
  интегрирования обязана сходиться; если нет, это самая ценная запись плана,
  и она идёт в `docs/wake.md` раньше любой правки.
- **Шаг 3 может повторить двенадцатую версию.** Разница от неё названа
  (полоска в системе, скос на соседей есть), но инвариант акватории надо
  мерить первым, а не последним.
- **Эталон шага 4 чужой.** Другая площадь, другой грот, другое Re, модельный
  масштаб с ламинарным пузырём на кромке (Souppez et al. 2019 предупреждают,
  что у моделей отрыв у задней кромки нереалистично ламинарный). Поэтому
  сравнение по форме кривой, полоса ±15 %, и два источника. Попадание в 2 %
  здесь не цель и объявляться не будет.
- **Шаг 5 подгоняет одну константу по одному стенду.** Это допустимо ровно
  потому, что CFD-зонд 1б остаётся вторым свидетелем на 35°; если оба нельзя
  удовлетворить одним K_v, правка не принимается.
- **Шаг 6 предсказуемо опасен**, и опасность записана в коде. Разделение
  «летящее пузо в геометрию, проектное в поправку» это обходит, но это
  гипотеза, и мерить её надо на бейдевинде, где риг складывался.

---

## Часть Б. Форма летящего паруса (добавлено 2026-09-11)

Шаги 0…9 выше про силы и устойчивость. Форму нарисованного генакера они не
чинят: шаг 6 лишь приведёт нарисованное пузо в согласие с решёткой, шаг 1
уберёт мигание у нуля угла. Всё остальное, из-за чего парус выглядит не как
генакер, лежит в геометрии обвода и в том, что мембрана у нас одномерная.
Ниже, что именно, и что с этим делать.

### Б0. Чем нынешняя форма отличается от настоящей

Снято с экрана на TWA 134…140°, ветер 6 м/с, шкот 5.5 м, и сверено с тем, как
устроен обвод в `scripts/build_physics.py` и отрисовка в `sim/main.js`.

1. **Серп на не той шкаторине.** Вся лишняя ширина (SHW/SFL = 0.85 против 0.5 у
   треугольника) положена в заднюю шкаторину: `GENNAKER_ROACH_K = 0.35`
   нижней, то есть дуга 1.46 м в корму. У настоящего несимметричного паруса
   задняя шкаторина почти прямая или слегка вогнутая, а ширина набирается
   ПЕРЕДНЕЙ шкаториной (luff round, 8…15 % её длины) и округлой нижней. На
   экране это и видно: большой круглый горб сзади, как у генуи-переростка, и
   прямая передняя.
2. **Плечо стоит не там.** Провис `GENNAKER_SAG` задан синусом в степени 0.8,
   максимум посередине высоты. У летящего паруса плечо выше, около 60…70 %
   высоты, а к фалу шкаторина заворачивается и сужается. Провис назначен
   числом 0.13, а не получен из запаса ткани: два описания одной вещи
   (серп обвода и провис полёта) не связаны между собой.
3. **Поверхность цилиндрическая.** Мембрана считает пузо по хорде, полоска за
   полоской; поперёк полосок кривизны нет. Настоящий парус выгнут в обе
   стороны, и объём, по которому его узнают, идёт от поперечной дуги между
   галсом и фалом.
4. **Шкотовый угол на палубе.** `clew` на высоте галса 0.78 м, нижняя
   шкаторина лежит горизонтально. На бакштаге у настоящего паруса шкотовый
   угол летит на метр-полтора выше, нижняя шкаторина приподнята и округла.
   Это следствие незамкнутой геометрии трёх шкаторин (§4.8 плана: 8.39 м
   заявленной задней против 9.21 построенной).
5. **Форма не дышит.** Пузо одной полоски меняется от нагрузки, но контур не
   отзывается ни на шкот, ни на курс: шкаторины в ДП те же, что у стакселя,
   провис тот же при любом ветре.

Что в порядке и трогать не надо: вынос фала 0.3 м и ширина у фала 0.25 м
поставлены против вырождения решётки и там остаются; заполаскивание в
картинке считается из запаса ткани и Струхаля, это правильно устроено.

### Б1. Обвод: ширина в переднюю шкаторину, а не в заднюю

**Правка.** Лишняя ширина делится между тремя шкаторинами именованными
долями, вместо одной `GENNAKER_ROACH_K`: передняя `LUFF_ROUND_K` (оценка
0.10…0.12 длины передней), нижняя `FOOT_ROUND_K` (0.04…0.06 нижней), задняя
`LEECH_ROUND_K` в пределах 0…0.03 нижней (у гоночных бывает и отрицательная).
Положение максимума дуги передней шкаторины 0.6…0.65 высоты, не 0.5. Площадь
27 м² и SHW/SFL = 0.85 остаются условиями: нижняя шкаторина по-прежнему
подбирается под площадь, и это решает сам `_gennaker`.

Обвод в ДП для полосок строится из этой же тройки: `chord(z) = luff(z) −
leech(z)` с дугами, а не отрезками. Серп передней шкаторины и есть та ткань,
которая в полёте уходит под ветер, поэтому величина провиса выводится из
него: `sag_max ≈ luff_round · k`, где k из натяжения (оценка, k ≈ 1 при
слабом фале); постоянная 0.13 уходит, и §Б0.2 закрывается тем же шагом.

**Свидетель.** `tests/gennaker.test.mjs`: площадь сходится с 27 м² (±0.05),
средняя ширина ≥ 0.75 нижней, вынос монотонен по шкоту, карта 25/25 без новых
красных клеток (обвод меняет хорды полосок и потому решётку). Новая проверка:
дуга задней шкаторины не больше 0.03 нижней.

**Ворота.** Бейдевинд без генакера побайтово (обвод грота и стакселя не
трогается). Ход на TWA 140° с генакером записать до и после: он изменится,
потому что изменились хорды, и это ожидаемо; порог не назначается, число
записывается.

**Цена.** День. Не зависит от части А.

### Б2. Трёхмерная ткань для картинки

**Правка.** Полотно генакера для отрисовки считается не по полоскам, а
тканью: сетка узлов (та же 11 строк по высоте × 8…10 по хорде, что у
`SAIL_ROWS`), ограничения по длинам рёбер (нерастяжимость), три угла: галс и
фал закреплены, шкотовый угол на связи длиной в шкот к погону
`sheet_lead_m`. Нагрузка на узлы: давление полоски, распределённое по хорде
как `q` из решётки (то же `q`, что идёт в `membraneCamber`), плюс ветер по
нормали для сложенных участков. Интегратор: позиционная динамика
(position-based dynamics), 8…12 итераций ограничений на кадр; на ста узлах
это сотые доли миллисекунды, в бюджет телефона входит.

Это только картинка: в силы ткань пока не идёт, решётка по-прежнему живёт на
полосках с одномерной мембраной. Ровно так же устроено заполаскивание сейчас.

**Свидетель.** Новый стенд `tests/cloth.test.mjs`: пузо ткани по каждой
полоске против `membraneCamber` той же полоски в пределах 20 % на
установившемся бакштаге (обе считают одну ткань из одного запаса длины, и
расходиться сильно им не с чего); провис передней шкаторины из ткани
записывается и сравнивается с `sag_max` из Б1, порог не назначается до
первого замера; высота шкотового угла над галсом на отданном шкоте не меньше
0.5 м (качественный признак с фотографий, оценка); ткань не дрожит на
установившемся режиме (размах узла за 5 с < 2 см).

**Ворота.** Бюджет кадра: `рисов.` в панели отладки не выше +0.3 мс.
Физика не меняется ни на байт: отпечаток по всем режимам побайтово, потому
что ткань ничего в силы не отдаёт.

**Цена.** Три дня.

### Б3. Ткань в решётку

**Когда делать.** После Б2 и после шага 6 части А, и только если карта
25/25 зелёная: это и есть аэроструктурная модель по образцу Cayon и Schmehl
(система частиц + нелинейная решётка), и она заменяет собой шаг 6.

**Правка.** Панели полоски строятся по срединной линии ткани в этой полоске
(пузо, провис, высота шкотового угла), с недорелаксацией между кадрами, как
в шаге 6. `GENNAKER_SAG`, `GENNAKER_SAG_POW`, `sagAt` и `slackOfSheet` для
генакера уходят: их считает ткань. Твист тоже: он выходит из того, куда ушёл
шкотовый угол, а не из `FREE_TWIST · slackOfSheet`.

**Свидетель.** Карта 25/25; стенд шага 4 (силы по AWA против эталона) до и
после; замкнутость трёх шкаторин: заявленная и построенная длина задней
совпадают (закрывает §4.8). Автоколебание ткань→решётка→ткань меряется той
же меркой g из §5.12 `docs/wake.md`: возмутить форму на ε, посмотреть отклик
Γ; |g| < 1.

**При красном.** Если петля ткань↔решётка раскачивается, ткань остаётся в
картинке (Б2), а в решётку идёт только её сглаженное пузо и провис с
постоянной времени `TRANSFER`; записать в `docs/wake.md`.

**Цена.** Неделя.

### Б4. Порядок

    Б1 → Б2 → (часть А, шаги 0…6) → Б3

Б1 и Б2 можно делать до части А и независимо от неё: Б1 меняет обвод, а не
метод, Б2 не трогает физику вовсе. Именно Б1 даст самое заметное изменение
картинки за день работы: плечо уйдёт вперёд и под ветер, горб сзади исчезнет.

---

## 5. Источники

Обзор и физика спинакера:
- Souppez, Arredondo-Galeana, Viola. Recent Advances in Downwind Sail
  Aerodynamics (CSYS 2019): LEV, пузыри, Re-эффекты, +50…70 % от flapping.
- Arredondo-Galeana, Viola. The leading-edge vortex of yacht sails (Ocean
  Eng. 2018): LEV даёт ≥ 25 % секционной подъёмной силы у топа.
- Viola, Flay. Sail pressures from full-scale, wind-tunnel and numerical
  investigations (Ocean Eng. 2011): силы RANS против трубы 0.5 %.
- Graf, Renzsch и др. An Experimental Validation Case for FSI Simulations of
  Downwind Sails: 6-компонентные силы и полётные формы по AWA и триму.
- Augier и др. Inviscid approach for upwind sails aerodynamics. How far can
  we go? (JWEIA 2016): невязкий VLM завышает кренящий момент при отрыве.
- Fluid–structure interaction of downwind sails: a new computational method
  (JMST 2018): слабая связь, генакер до 9.47 м/с, сходится без итераций.

Быстрые методы, из которых берутся шаги:
- Cayon, Gaunaa, Schmehl. Fast Aero-Structural Model of a Leading-Edge
  Inflatable Kite (Energies 2023) и библиотека Vortex-Step-Method (awegroup,
  MIT): нелинейная несущая линия на ¾ хорды + 2D вязкие поляры + система
  частиц.
- Leuthold. Multiple-Wake Vortex Lattice Method for Membrane-Wing Kites (TU
  Delft): пелены с известных линий отрыва, ±10 % от RANS.
- Ramesh и др. Discrete-vortex method with novel shedding criterion (JFM
  2014) и Reduced-Order Discrete-Vortex Method (AIAA 2022): LESP, N-LEV.
- Katz, Plotkin. Low-Speed Aerodynamics: правило длины кольца пелены.

Форма летящего паруса (часть Б):
- Cayon, Gaunaa, Schmehl (там же): система частиц для ткани и строп,
  двусторонняя связь с решёткой.
- Renzsch, Graf. Fluid-structure interaction simulation of spinnakers /
  валидационный случай: полётные формы по фотограмметрии как эталон формы.
- Deparday и др. Full-scale flying shape measurements of spinnakers:
  положение плеча, высота шкотового угла, заворачивание у фала.
- Müller, Chentanez и др. Position Based Dynamics (2007): интегратор ткани
  для Б2.
